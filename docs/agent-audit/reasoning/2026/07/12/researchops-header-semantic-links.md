# ResearchOps header semantic links

The shared homepage/logo and skip links were the remaining header anchors without Flux semantics. They now use stable publisher-controlled keys; account and main-navigation links retain their existing keys.
