# Agent trace — Outcomes GOV.UK template migration

**Date:** 2026-06-02  
**Branch:** `feature/outcomes-govuk-template`  
**Mode:** `rops-build`  
**Trace type:** operational audit trace

## Evidence boundary

This trace records repository sources inspected, implementation decisions, files changed and validation status. It does not expose private chain-of-thought.

## Task

Move `/pages/projects/outcomes/` to a Nunjucks source template that uses GOV.UK Frontend macros and renders a fully compliant GOV.UK Frontend UI.

## Operating model and bundles applied

Loaded sources included:

- `README.md`
- `AGENTS.md`
- `RECENT_LEARNINGS.md`
- `.agent-operating-model/orchestration.xml`
- `.agent-operating-model/bundle-registry.json`
- `.agent-operating-model/task-signal-catalog.json`
- `.agent-operating-model/selection-rules.json`
- `.agent-operating-model/github-mutation-policy.md`
- `.agent-operating-model/bundles/github/prompt.spec.yaml`
- `.agent-operating-model/bundles/github/prompt.body.xml`
- `.agent-operating-model/bundles/researchops-developer-control/prompt.spec.yaml`
- `.agent-operating-model/bundles/researchops-developer-control/prompt.body.xml`
- `.agent-operating-model/bundles/multi-functional-team/prompt.spec.yaml`
- `.agent-operating-model/bundles/multi-functional-team/prompt.body.xml`
- `.agent-operating-model/bundles/govuk-design-system/prompt.spec.yaml`
- `.agent-operating-model/bundles/govuk-design-system/prompt.body.xml`
- `.agent-operating-model/bundles/cloudflare/prompt.spec.yaml`
- `.agent-operating-model/bundles/cloudflare/prompt.body.xml`

Selected bundles:

- GitHub Diamond
- ResearchOps Developer Control
- Multi-Functional Team
- GOV.UK Design System
- Cloudflare

Task signals:

- `repository-affecting-task`
- `government-product-assurance-default`
- `ui-or-content-change`
- `runtime-or-deployment-change`

## Findings

The current outcomes page was mixed-mode. It loaded GOV.UK Frontend, legacy GOV.UK CSS partials and `/css/screen.css` at the same time.

The page hand-coded breadcrumbs, form groups, inputs, selects, textarea, table and buttons. It did not have a Nunjucks source template in the GOV.UK page renderer.

The impact tracker script used `alert("Metric name is required")` for required field validation. That does not match a GOV.UK Frontend form error pattern.

The repository render workflow is configured to render and commit static GOV.UK page outputs when page templates or the renderer change. This means `public/pages/projects/outcomes/index.html` should be generated by the workflow after the PR opens, not edited manually in this branch.

## Decisions

1. Add `src/govuk/templates/pages/projects-outcomes.njk` as the source template for `/pages/projects/outcomes/`.
2. Use GOV.UK macros for breadcrumbs, buttons, error summary, input, inset text, select and textarea.
3. Keep the existing IDs used by page scripts, including `impact-tracker`, `impact-form`, `impact-table`, `impact-metricName` and `impact-insightId`.
4. Register the new template in `scripts/govuk/render-govuk-pages.mjs`.
5. Do not hand-edit `public/pages/projects/outcomes/index.html`. Let the render workflow produce it from the template.
6. Replace browser alert validation in `impact-tracker.js` with a GOV.UK error summary path.
7. Update route-state expectations so outcomes is treated as a GOV.UK Frontend-rendered route, not a legacy `govuk-forms.css` route.

## Changes made

Created:

- `src/govuk/templates/pages/projects-outcomes.njk`
- `docs/agent-audit/reasoning/2026/06/02/outcomes-govuk-template.json`
- `docs/agent-audit/reasoning/2026/06/02/outcomes-govuk-template.md`

Modified:

- `scripts/govuk/render-govuk-pages.mjs`
- `public/components/impact-tracker.js`
- `tests/govuk-forms-application-route-state.test.js`

## Validation status

Local validation was not run in this connector-only session.

Recommended commands:

```sh
npm run build:govuk-pages
node --test tests/govuk-forms-application-route-state.test.js
npm test
npm run validate
```

Expected automation:

The `Render GOV.UK pages` workflow should render `public/pages/projects/outcomes/index.html` after the PR opens because `src/govuk/templates/pages/projects-outcomes.njk` and `scripts/govuk/render-govuk-pages.mjs` changed.

## Manual preview validation targets

Use a known project ID:

- `/pages/projects/outcomes/?id=<project-record-id>`
- confirm the page renders with GOV.UK Frontend assets only;
- confirm breadcrumbs resolve the project dashboard link;
- submit the impact form with Metric name empty and confirm `#impact-error-summary` links to `#impact-metricName`;
- submit a valid impact record and confirm the impact table refreshes.

## Residual risks

Until the render workflow commits generated output, `public/pages/projects/outcomes/index.html` on the branch remains the pre-rendered version.

No browser preview was available in this connector-only session, so focus behaviour and form submission still need branch-preview confirmation.
