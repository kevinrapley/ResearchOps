# === Build + deps ===
node_modules/
dist/
coverage/
.tmp/
.logs/
*.log

# === Lockfiles & caches ===
package-lock.json
npm-shrinkwrap.json

# === CI / metadata / prose ===
.github/**
docs/**
README.md

# === Config blobs & schemas we don't want Prettier to fight ===
.pa11yci.json
config/**

# === Serverless/worker infra & functions (format later, optional) ===
infra/** 
functions/**

# === Public site (mix of vendor, HTML, CSS; format later if desired) ===
public/**

# === Scripts & misc (optional) ===
scripts/**

# === SDK and jobs (optional – re-enable when ready) ===
src/sdk/**
src/jobs/**
