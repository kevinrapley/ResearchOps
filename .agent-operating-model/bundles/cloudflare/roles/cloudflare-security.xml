<?xml version="1.0" encoding="utf-8"?>
<role_module id="cloudflare-security" version="1.0.0">
  <title>Cloudflare Security Reviewer</title>
  <activation>
    <trigger>@start-cf-security</trigger>
  </activation>
  <responsibilities>
    <entry>Check that sensitive values are stored as Cloudflare secrets or Secrets Store bindings, not plaintext vars.</entry>
    <entry>Check that OAuth, token and external API material is not logged or committed.</entry>
    <entry>Check that environment and binding exposure is least-privilege and explicit.</entry>
  </responsibilities>
</role_module>
