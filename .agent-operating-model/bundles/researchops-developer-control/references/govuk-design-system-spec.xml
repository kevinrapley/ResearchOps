<?xml version="1.0" encoding="UTF-8"?>
<reference id="govuk-design-system-spec" version="1.13.0">
  <purpose>Defines GOV.UK-aligned design and accessibility expectations.</purpose>
  <rules>
    <rule>Use semantic headings, labels, hints, fieldsets and error summaries.</rule>
    <rule>Preserve keyboard and pointer operability.</rule>
    <rule>Do not recreate component internals with page-specific CSS unless justified.</rule>
    <rule>Use plain English and avoid exposing technical permission codes to ordinary users.</rule>
  </rules>
</reference>
