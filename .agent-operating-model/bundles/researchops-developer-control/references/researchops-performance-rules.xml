<?xml version="1.0" encoding="UTF-8"?>
<reference id="researchops-performance-rules" version="1.13.0">
  <purpose>Defines performance, CSS and caching rules for ResearchOps.</purpose>
  <rules>
    <rule>Maintain `docs/performance/initial-load-audit.md` when performance work changes load behaviour.</rule>
    <rule>Do not remove global CSS contracts to optimise a single route.</rule>
    <rule>Distinguish global CSS, shared pattern CSS, route CSS and obsolete selectors.</rule>
    <rule>Use Cloudflare Pages cache headers carefully: long-lived immutable assets, no-store for app pages where needed.</rule>
    <rule>Defer shared layout modules where this does not break page chrome or accessibility.</rule>
  </rules>
</reference>
