<?xml version="1.0" encoding="UTF-8"?>
<reference id="researchops-design-patterns" version="1.13.0">
  <purpose>Defines reusable ResearchOps page and interaction patterns.</purpose>
  <patterns>
    <pattern id="project-context">Pages scoped to a project must preserve project identity and return routes.</pattern>
    <pattern id="study-context">Pages scoped to a study must preserve both project and study identity.</pattern>
    <pattern id="empty-state">Empty states must explain what is missing and what the user can do next.</pattern>
    <pattern id="check-before-write">High-impact actions should validate and show a check step before write.</pattern>
  </patterns>
</reference>
