<?xml version="1.0" encoding="UTF-8"?>
<reference id="mural-api-spec" version="1.13.0">
  <purpose>Defines Mural integration expectations for ResearchOps.</purpose>
  <rules>
    <rule>Duplicate the reflexive journal board from the configured template when required.</rule>
    <rule>Create or use a project-named folder in the user's private room where the integration requires it.</rule>
    <rule>Keep sticky-note category mapping explicit.</rule>
    <rule>Do not silently fall back to the wrong room or board.</rule>
  </rules>
</reference>
