<?xml version="1.0" encoding="UTF-8"?>
<reference id="core-rules" version="1.13.0">
  <rules>
    <rule id="inspect-first">Inspect existing files, routes, fixtures and conventions before implementing.</rule>
    <rule id="do-not-invent">Do not invent endpoint shape, table names, field names or runtime guarantees.</rule>
    <rule id="component-layer">Identify the correct implementation layer before editing.</rule>
    <rule id="truthful-status">Do not state that work is complete, merged, deployed or validated without observable evidence.</rule>
    <rule id="batch-visible-work">For repository uploads, keep branch and PR state visible and avoid hidden work claims.</rule>
  </rules>
</reference>
