<?xml version="1.0" encoding="UTF-8"?>
<reference id="cloudflare-development-spec" version="1.13.0">
  <purpose>Defines Cloudflare Workers and Pages expectations for ResearchOps.</purpose>
  <rules>
    <rule>Distinguish static Pages assets from Worker API routes.</rule>
    <rule>Keep same-origin `/api/*` calls compatible with Pages previews unless an explicit API origin is configured.</rule>
    <rule>Do not claim deployment success without workflow or platform evidence.</rule>
    <rule>Use no-store headers for app pages where stale UI state could mislead users.</rule>
  </rules>
</reference>
