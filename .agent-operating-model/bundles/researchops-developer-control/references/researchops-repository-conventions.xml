<?xml version="1.0" encoding="UTF-8"?>
<reference id="researchops-repository-conventions" version="1.13.0">
  <purpose>Defines repository conventions.</purpose>
  <rules>
    <rule>Static pages live under `public/pages/`.</rule>
    <rule>Page JavaScript lives under `public/js/`.</rule>
    <rule>Route-specific CSS should not override GOV.UK component internals unnecessarily.</rule>
    <rule>Worker route logic lives under the Cloudflare source tree.</rule>
    <rule>Tests may include contract, regression and route-state checks.</rule>
  </rules>
</reference>
