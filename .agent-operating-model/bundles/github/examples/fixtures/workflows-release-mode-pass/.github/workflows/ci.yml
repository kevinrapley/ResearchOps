name: CI (Release Mode Pass Fixture)

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  release-mode-fixture:
    name: Validate release-mode workflow fixture
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository with reviewed SHA pin
        uses: actions/checkout@1234567890abcdef1234567890abcdef12345678

      - name: Confirm release-mode fixture evidence
        run: |
          test -f workflow-action-lock.yaml
          grep -q "actions/checkout" workflow-action-lock.yaml
          grep -q "1234567890abcdef1234567890abcdef12345678" workflow-action-lock.yaml

      - name: Confirm workflow hardening purpose
        run: |
          test -f README.md
          grep -q "Release-mode workflow hardening pass fixture" README.md
