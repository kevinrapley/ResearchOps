budgets:
  - metric: response_time_p95_ms
    threshold: 300
    tolerance_percent: 5
    unit: ms
    mode: blocking
  - metric: bundle_size_kb
    threshold: 250
    tolerance_percent: 3
    unit: kb
    mode: advisory
