<?xml version='1.0' encoding='utf-8'?>
<reference id="performance-budget-control" version="2.9.1" xml_profile="native-semantic-xml/v1">
  <title>Performance Budget Control</title>
  <description source="internal-doctrine">Defines executable performance budget controls.</description>
  <rules>
    <rule id="perf-001" source="internal-doctrine">Performance-sensitive repositories must define performance-budget.yaml.</rule>
    <rule id="perf-002" source="internal-doctrine">Benchmark results must be written to performance-results.yaml using the measurements array shape.</rule>
    <rule id="perf-003" source="internal-doctrine">CI must run scripts/check-performance-budget.py with named arguments.</rule>
    <rule id="perf-004" source="internal-doctrine">Budget failures block release unless a dated waiver and gap-register entry exist.</rule>
  </rules>
</reference>