{
  "tool_name": "createResearchNote",
  "server": "researchops-mcp-server",
  "side_effects": true,
  "argument_validation": ["validate project ID", "validate note body length", "reject unknown participant IDs"],
  "result_validation": ["confirm record ID shape", "confirm created timestamp", "record source tool call"],
  "consent": ["show user the note body before creation", "require explicit confirmation"],
  "audit": ["correlation ID", "tool name", "server", "confirmed by user", "timestamp"],
  "risks": ["incorrect project linkage", "accidental disclosure", "duplicate record creation"]
}
